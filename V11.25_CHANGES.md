# v11.25 变更

- 实时后台不再每3秒读取31名学生的完整消息历史；新增 `/live-statuses` 轻量状态接口。
- 自动刷新改为完成一轮后再等待4秒，避免请求堆积；“立即刷新”显示刷新中/成功时间/失败原因。
- 当前学生详情采用增量获取：只拉取新增聊天、行为事件、任务版本。
- 新增右侧“实时行为轨迹”：开始输入、删除未发送、草稿未发送、发送、回看旧回复、AI处理/完成/异常、任务文本版本修改。
- 未发送草稿正文仍不保存，只展示字符数/时长/编辑次数等元数据。
- 学生列表新增“正在输入/取消发送/草稿未发”短时状态与最近已发送问题预览。
- `appendMessage` 不再为分配序号读取全部历史消息正文，降低聊天越用越慢的问题。
- 保留 S00 监看，但 S00 继续排除在正式研究导出与统计之外。

- 修复真正的性能瓶颈：实时轮询不再每次“确保30名学生”而触发30次读+30次写；历史13周汇总不再自动加载，也不再逐条读取全部聊天正文。
- 历史课次改为需要时点击刷新读取，避免后台打开时被大量Blob请求拖住。
