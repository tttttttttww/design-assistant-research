document.getElementById('loginForm').onsubmit=async e=>{e.preventDefault();const id=document.getElementById('participantId').value.trim().toUpperCase(),box=document.getElementById('error');box.classList.add('hidden');try{const r=await App.api('/validate',{method:'POST',body:JSON.stringify({participantId:id})});App.setParticipantId(r.participant_id);location.href='/task.html';}catch(x){box.textContent=x.message;box.classList.remove('hidden');}};
