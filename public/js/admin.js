const api=async(path,options={})=>{const r=await fetch('/express/api/admin'+path,{headers:{'Content-Type':'application/json',...(options.headers||{})},...options});let d={};try{d=await r.json();}catch{}if(!r.ok)throw new Error(d.error||'操作失败');return d;};
let rows=[],settings=null,sessions=[];
async function check(){try{await api('/settings');document.getElementById('login').classList.add('hidden');document.getElementById('app').classList.remove('hidden');await loadSettings();await loadParticipants();}catch{}}
document.getElementById('loginForm').onsubmit=async e=>{e.preventDefault();try{await api('/login',{method:'POST',body:JSON.stringify({password:document.getElementById('password').value})});document.getElementById('login').classList.add('hidden');document.getElementById('app').classList.remove('hidden');await loadSettings();await loadParticipants();}catch(x){const b=document.getElementById('loginError');b.textContent=x.message;b.classList.remove('hidden');}};
document.getElementById('logout').onclick=async()=>{await api('/logout',{method:'POST'});location.reload();};
async function loadSettings(){settings=await api('/settings');sessions=settings.sessions||[];const f=document.getElementById('settingsForm');f.active_session_id.innerHTML=sessions.map(s=>`<option value="${s.id}">${s.id} · ${s.date} · ${App.escapeHtml(s.title)}</option>`).join('');f.active_session_id.value=settings.active_session_id;f.session_open.checked=Boolean(settings.session_open);f.questionnaire_enabled.checked=Boolean(settings.questionnaire_enabled);renderSessionCards();}
document.getElementById('settingsForm').onsubmit=async e=>{e.preventDefault();const f=e.target;try{await api('/settings',{method:'POST',body:JSON.stringify({active_session_id:f.active_session_id.value,session_open:f.session_open.checked,questionnaire_enabled:f.questionnaire_enabled.checked})});alert('设置已保存');await loadSettings();await loadParticipants();}catch(x){alert(x.message);}};
function renderSessionCards(){document.getElementById('sessionCards').innerHTML=sessions.map(s=>`<div class="session-card ${s.id===settings.active_session_id?'current':''}"><div class="row between"><strong>${s.id} · ${s.date}</strong><span class="badge">${s.ai_mode==='condition'?'按组AI':s.ai_mode==='free'?'自由AI':'无AI'}</span></div><h3>${App.escapeHtml(s.title)}</h3><p>${App.escapeHtml(s.subtitle)}</p><p class="small">${App.escapeHtml(s.brief.join(' '))}</p>${s.candidate?'<div class="notice">候选任务：正式实施前仍可替换。</div>':''}<button class="btn ghost set-current" data-id="${s.id}">设为当前课次</button></div>`).join('');document.querySelectorAll('.set-current').forEach(b=>b.onclick=async()=>{await api('/settings',{method:'POST',body:JSON.stringify({active_session_id:b.dataset.id,session_open:settings.session_open,questionnaire_enabled:settings.questionnaire_enabled})});await loadSettings();await loadParticipants();});}
document.getElementById('createDefault').onclick=async()=>{await api('/participants/create-default',{method:'POST'});await loadParticipants();};
document.getElementById('bulkBtn').onclick=async()=>{const r=await api('/participants/bulk-meta',{method:'POST',body:JSON.stringify({text:document.getElementById('bulkText').value})});const bad=r.result.filter(x=>x.status!=='ok');alert(`处理 ${r.result.length} 行${bad.length?`，${bad.length} 行无效`:''}`);await loadParticipants();};
function badge(v,t='已完成'){return v?`<span class="badge">${t}</span>`:'—';}
async function loadParticipants(){rows=await api('/participants');const formal=rows.filter(x=>!x.is_test),sid=settings?.active_session_id||'';document.getElementById('currentTitle').textContent=`${sid} 课堂数据总览`;document.getElementById('summary').innerHTML=[['正式学生',formal.length],['已进入任务',formal.filter(x=>x.started).length],['使用过AI',formal.filter(x=>x.ai_used).length],['已提交',formal.filter(x=>x.submitted).length]].map(([k,v])=>`<div class="summary-card"><strong>${v}</strong><span>${k}</span></div>`).join('');document.getElementById('participantTable').innerHTML=`<div class="table-wrap"><table class="admin-table"><thead><tr><th>编号</th><th>年级</th><th>condition</th><th>进入任务</th><th>首次打开AI</th><th>首次发消息</th><th>AI消息</th><th>提交</th></tr></thead><tbody>${rows.map(x=>`<tr data-id="${x.participant_id}" class="p-row"><td><strong>${x.participant_id}</strong>${x.is_test?' <span class="badge">测试</span>':''}</td><td>${App.escapeHtml(x.grade||'')}</td><td>${x.condition}</td><td>${badge(x.started)}</td><td>${x.first_ai_open_latency_seconds==null?'—':x.first_ai_open_latency_seconds+'s'}</td><td>${x.first_user_message_latency_seconds==null?'—':x.first_user_message_latency_seconds+'s'}</td><td>${x.user_turn_count}</td><td>${badge(x.submitted)}</td></tr>`).join('')}</tbody></table></div>`;document.querySelectorAll('.p-row').forEach(tr=>tr.onclick=()=>detail(tr.dataset.id));}
function image(id,sid,key,p){return p?`<a target="_blank" href="/express/api/image/${id}/${sid}/${key}/${encodeURIComponent(p.file_name)}"><img src="/express/api/image/${id}/${sid}/${key}/${encodeURIComponent(p.file_name)}"></a>`:'';}
function messages(arr=[]){return arr.map(m=>`<div class="answer-block"><strong>${m.role==='assistant'?'AI':'学生'} · ${m.message_index}</strong><p>${App.escapeHtml(m.content)}</p></div>`).join('')||'<p class="small">暂无聊天记录</p>';}
async function detail(id){const d=await api(`/participant/${id}`);document.getElementById('detail').innerHTML=`<span class="eyebrow">完整记录</span><h2>${id}${d.participant.is_test?' · 测试号':''}</h2><div class="section"><h3>基本信息</h3><div class="grid two"><label>年级<input id="grade" value="${App.escapeHtml(d.participant.grade||'')}"></label><label>condition<select id="condition"><option value="unassigned">unassigned</option><option value="A">A · 支持型AI</option><option value="B">B · 自由AI</option></select></label></div><button class="btn secondary" id="saveMeta">保存</button></div><div class="section"><h3>12课时记录</h3>${sessions.map(s=>{const x=d.sessions[s.id],r=x.record,arts=Object.entries(r.artifacts||{});return `<details class="answer-block"><summary><strong>${s.id} ${App.escapeHtml(s.title)}</strong> ${r.submitted_at?'<span class="badge">已提交</span>':''}</summary><p class="small">AI变体：${App.escapeHtml(r.ai_variant||'')}｜首次打开：${r.first_ai_open_latency_seconds??'—'}s｜首次发消息：${r.first_user_message_latency_seconds??'—'}s</p>${Object.entries(r.text_fields||{}).map(([k,v])=>`<p><strong>${App.escapeHtml(k)}：</strong>${App.escapeHtml(v)}</p>`).join('')}${arts.length?`<div class="photo-grid">${arts.map(([k,p])=>image(id,s.id,k,p)).join('')}</div>`:''}<h4>AI聊天</h4>${messages(x.chat_messages)}</details>`;}).join('')}</div>`;document.getElementById('condition').value=d.participant.condition||'unassigned';document.getElementById('saveMeta').onclick=async()=>{await api(`/participant/${id}/meta`,{method:'POST',body:JSON.stringify({grade:document.getElementById('grade').value,condition:document.getElementById('condition').value})});await loadParticipants();await detail(id);};}
check();
