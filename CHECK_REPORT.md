# v11.11 检查说明

本版重点检查：

- 12课时配置和原有数据链保持不变；
- 学生继续使用“编号 + 姓名”双重核对；
- 教师后台支持 `.xlsx` / `.csv` 名单文件上传；
- Excel/CSV 只在内存解析，原文件不持久化；
- 导入前预览 S01–S30、姓名、年级、condition；
- 自动检查错误编号、重复编号、缺失编号、空姓名、非法年级和非法 condition；
- 同名学生自动提示；
- 确认导入后只保存姓名哈希，不保存姓名明文；
- 正式研究导出继续排除 S00 且不包含姓名/姓名哈希；
- v11.7 表单状态修复继续保留；
- 管理员端完整12课时记录、聊天图片、任务图片和两类 ZIP 导出继续保留。

已用标准 `.xlsx`（30人、编号/姓名/年级/condition）验证内置解析器：30/30 可识别并通过完整导入检查。
