# v11.21 检查要点

- 13周课程配置保留；W2统一 `free`，W3–W8 `condition`，W9–W11统一 `free`。
- W2无前测/分组门槛；未分组学生可直接做共同试用任务。
- W2主任务为 Stanford d.school 学校午餐体验课堂适配；第二个可选加练为 TeachEngineering Marisol 储物柜案例。
- W2两项任务均纯网页、零材料、无最终图片上传。
- 删除“从最不确定/最想比较的地方问AI”等求助策略提示，仅保留中性的“本节至少使用1次AI，时机和内容自主决定”。
- AI输入草稿只记录开始/删除未发送/离开未发送/发送等元数据，不保存未提交草稿全文。
- 新增 `chat_history_revisit` 原始事件，需后续人工决定是否编码为回看帮助。
- 新增任务文本版本历史；相同内容不重复记录，版本可关联最近AI回复及时间间隔。
- 主导出包新增 `task_revisions.csv`、`behavior_events.csv`；完整JSON包含 revisions。
- Blob 根前缀未改，W1旧数据不会因代码部署自动删除。
