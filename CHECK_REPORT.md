# v11.19 检查报告
- 13周配置完整；W2统一 `free`，W3–W8 `condition`，W9–W11统一 `free`，W12–W13无AI。
- W2没有前测硬门槛、没有分组门槛；condition=`unassigned` 的正式学生可以正常使用普通AI。
- W2任务来源改为 Stanford d.school `Redesign the School Lunch Experience` 的课堂适配版；统一提供任务学校背景、6段午餐体验记录和可改变范围，不要求外部检索。
- W2前半段包含：证据/需要、优先问题、第一版问题定义、3个不同方案方向、初步选择。
- W2明确要求第一版问题定义后至少进行1次真实普通AI咨询；无服务器硬门槛，避免AI故障阻塞提交。
- 新增中途统一反馈机制：前5题未完成时不能展开新反馈；展开后记录 `mid_task_update_revealed` 事件，再填写修改决定和最终方案。
- W3正式辅助绘画主项目不 `carry_from: W2`，避免午餐试用内容进入正式项目。
- W1旧记录存储前缀未变；更新代码不会主动清空旧数据。继续保留旧数据的前提是同一项目、同一 `BLOB_STORE_NAME`、同一 `EXPERIMENT_RUN_ID`，且不执行整批更换名单/重置。
- 后台保留聊天、聊天图片、任务记录、作品图片、JSON备份等原始数据导出。
