# v11 检查说明

目标检查项：
- 12课时配置完整；
- S00 / S01-S30规则保留；
- AI从任务开始即可打开，不再由第一版上传解锁；
- 记录首次打开AI与首次发送消息时延；
- W4-W9按condition路由双智能体；
- W10-W11自动恢复自由AI；
- W12无AI；
- 所有课次文字、图片、聊天、事件可导出；
- W2/W10/W11标注为候选任务；
- 问卷题项未提前写入。

运行 `npm run check` 和 `npm test` 做本地静态与逻辑检查。
