# v11.21 今天下午上课前部署

1. 用本 ZIP 更新当前 GitHub 项目并等待 EdgeOne Pages 部署完成。
2. **不要修改 `BLOB_STORE_NAME` 或 `EXPERIMENT_RUN_ID`**；这样 W1 旧记录继续保留。
3. 如果还是同一批学生，不要点“整批更换学生名单”。
4. 后台把当前课次设为 **W2** 并开放。W2不需要A/B分组，也不做前测。
5. 先用 S00 测一次完整流程：
   - 登录；
   - 填主任务前5题；
   - 任意时机打开普通AI并成功发送1条；
   - 查看新反馈；
   - 填第6、7题；
   - 主任务完成后点“有时间再做”检查储物柜加练能正常展开；
   - 输入一段AI草稿后全部删除，确认后台事件里出现 `ai_draft_deleted_unsent`；
   - 修改一个任务文本框两次，确认完整JSON/导出里出现 `revisions`；
   - 最后提交。
6. 正式上课只统一说明：**“本节会使用右侧AI助手。请至少实际使用1次；具体什么时候使用、问什么、是否继续使用，由你自己决定。”** 不提示求助时机或求助策略。
7. 课堂约50分钟：午餐主任务为必做；储物柜为有时间才做的可选加练。两项都无需材料、无需图片作业。
8. 课后下载“AI聊天+聊天图片 ZIP”“任务记录+学生作品 ZIP”和完整JSON。第二个ZIP会额外含 `task_revisions.csv` 与 `behavior_events.csv`。
9. W2数据作为试行语料，用于检查平台、修订编码手册和实验组人设；W3前再完成分层随机。
