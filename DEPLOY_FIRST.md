# v11.9 部署顺序

1. 用本 ZIP 覆盖 GitHub 当前项目并重新部署。
2. 打开 `/health`，应显示 `course-v11.9`。
3. 打开 `/admin.html`，进入“学生名单、年级与后续分组”。
4. 点击 **选择名单文件**，上传现成 `.xlsx` 或 `.csv`。
5. 后台会先预览并检查。只有识别到 S01–S30 共 30 人且没有格式错误时，“确认导入30人”按钮才可用。
6. 核对姓名、年级后点击确认导入。后台总览应显示 **姓名校验已设置 30/30**。
7. 用一个正式编号测试：错误姓名不能进入，正确姓名可以进入。
8. S00 仍是测试号，姓名填写“测试”即可。
9. 正式上课前再检查当前课次、是否开放、A/B 分组状态。

## 名单隐私

- Excel/CSV 原文件不会写入 Blob，也不会保存在平台。
- 预览阶段姓名只显示在已登录的教师后台中。
- 确认导入后只保存“编号 + 标准化姓名”的 SHA-256 哈希。
- 姓名明文和姓名哈希都不进入聊天 CSV、任务 CSV、正式 JSON，也不发送给 AI。

## 如果名单识别失败

优先检查第一行是否包含“学生编号/编号”和“姓名”。也可以把 Excel 另存为 `.csv` 后再上传。后台仍保留“手动批量粘贴”作为备用。
