# v11.6 部署顺序

1. 用本ZIP内容替换当前GitHub项目，推送后等待腾讯云 EdgeOne Pages 重新部署。
2. 在腾讯云环境变量中确认：
   - `COZE_ACCESS_TOKEN`
   - `COZE_FREE_BOT_ID`
   - `COZE_SUPPORTED_BOT_ID`
   - `ADMIN_PASSWORD`
   - `BLOB_STORE_NAME`
   - `EXPERIMENT_RUN_ID=design-thinking-course-202609-v11-enhanced`
   - `COZE_ACCESS_TOKEN` 除聊天/会话权限外，还要授权“文件-uploadFile”权限，聊天图片才能先上传到Coze并取得 file_id。
   - `PUBLIC_BASE_URL` 可以留空；v11.3 默认不再依赖公网图片 URL。
3. 两只Coze智能体使用同一个**支持图片理解**的豆包模型，并保持模型参数、知识库/联网设置一致。
4. 打开 `/health`，应显示 `course-v11.6`。
5. 打开首页，应能明显看到“进入教师后台”；也可直接访问 `/admin.html`。
6. 后台先把当前课次设为 W1，并保持“向学生开放当前课次”。
7. 用 S00 测试：
   - 直接进入任务；
   - 打开AI；
   - 只发文字一次；
   - 再选择一张草图/照片并配文字发送；
   - AI应能针对图片正常回复；
   - 后台应看到聊天图片数、消息内容和图片缩略图。
8. 再测试任务页作品图片上传、保存、提交，后台应能完整查看。
9. 后台依次切换 W2-W12 预览全部任务。
10. W4-W9测试双智能体路由：
    - 把S00 condition改为A，确认走支持型Bot；
    - 再改B，确认走自由Bot。
11. W10/W11无论A/B都应回到自由Bot。
12. 点击数据导出，分别下载“AI聊天 + 聊天图片 ZIP”和“任务记录 + 学生作品 ZIP”，确认 CSV 与对应原图都在包内；再下载完整 JSON 作为备份。

## S00注意

- S00重新从学生登录页进入时，会归档并清空上一轮S00活动记录。
- 如果只是连续预览12课时，不要重新登录S00；在后台切课次后刷新学生任务页即可。
- S00不进入正式导出。

## 正式上课前必须检查

- 两只Bot都真的支持图片理解；
- 图片发给AI后，AI没有说“我看不到图片”；
- A/B除系统提示词外，其余配置一致；
- `/admin.html` 密码可正常登录；
- 正式学生编号 S01-S30 已补齐；
- 当前课次和“开放当前课次”状态正确；
- W2/W10/W11候选任务是否已经冻结。


## 误触/测试数据清理
后台“当前课次数据”表每行有“重置本课次”。需要整节课重新开始时，可点“重置当前课次全部记录”，并按提示输入 `RESET Wn`。系统会先归档旧记录，再清空当前活动记录。

## v11.6 升级后先做一次后台验证

- 浏览器强制刷新一次后台页面；本版资源版本号已改为 `v=11.6`，之后不会继续命中旧版 `admin.js` 缓存。
- 在当前课次用 S01 留下一条任务文字、一张任务作品、一轮AI文字消息和一轮AI图片消息。
- 回后台点击 **S01** 或“查看记录”，应先出现加载提示，随后当前课次自动展开并显示上述全部内容。
- 本版没有修改 `EXPERIMENT_RUN_ID` 或存储根路径，v11.5 已保存的数据不会因为升级而换到新库。
