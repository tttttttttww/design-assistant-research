# v11.18 部署与明日上课顺序

1. 用本ZIP替换GitHub当前项目并等待腾讯云 EdgeOne Pages 重新部署。
2. 确认 `COZE_ACCESS_TOKEN`、`COZE_FREE_BOT_ID` 和存储环境变量仍有效。**不要修改 `BLOB_STORE_NAME` 或 `EXPERIMENT_RUN_ID`**；否则旧记录不会出现在当前数据命名空间中。明天W2只使用普通AI，不需要提前完成A/B分组。
3. 后台核对 S01–S30 的“编号+姓名+年级”。如果只是沿用上周同一批学生，**不要点“整批更换名单”**；该功能会清空正式学生的当前活跃数据。
4. 当前课次设为 **W2**，开放当前课次。
5. 用 S00 完整测试：登录 → 阅读任务资料包 → 填写6个问题中的草稿 → 打开普通AI并发送1条消息 → 保存 → 提交。
6. 正式上课：学生完成点名/登录后直接进入W2“小行星来袭”共同试行任务；页面不会出现前测，也不会要求等待分组。
7. 教师统一说明：任务内容中的判断和方案不直接给答案；设备、登录和页面操作问题可以问老师。学生是否使用AI、什么时候使用、问什么，由学生自己决定。
8. 课后下载“AI聊天 + 聊天图片 ZIP”“任务记录 + 学生作品 ZIP”，检查是否完整保存。
9. W2结束后再根据实际课堂和学生自然AI对话，冻结正式编码边界与实验组人设；正式A/B分组安排在W3前完成。
